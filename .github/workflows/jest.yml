name: Jest
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2

      - name: Install
        run: npm ci

      - name: Jest
        run: npm run test:jest
